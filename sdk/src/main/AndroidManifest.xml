<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application>
        <activity
            android:name=".ui.WalkThroughActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Transparent" />
        <activity
            android:name=".ui.SmartSpectraActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Material3.Light.NoActionBar" />

        <provider
            android:name=".crashes.InitializationContentProvider"
            android:authorities="${applicationId}.com.presagetech.smartspectra.crashes_initialization_provider"
            android:exported="false"
            android:enabled="true">
        </provider>
    </application>
</manifest>
